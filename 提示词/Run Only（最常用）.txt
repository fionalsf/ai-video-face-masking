本次任务是 Run Only。

禁止修改任何代码。

禁止新增任何代码。

禁止删除任何代码。

禁止修改任何配置。

禁止自动修复任何 Bug。

禁止调整任何参数。

禁止安装或升级任何依赖。

禁止修改 Pipeline。

禁止修改任何 json schema。

禁止修改任何 Python 文件。

你的任务只有：

使用当前代码，运行 Production Pipeline。

处理视频：

DJI_xxxxx.mp4

运行结束后：

请输出：

1. 本次实际执行的 Pipeline。

2. 每一步耗时。

3. 是否出现 Error。

4. 输出目录。

5. 生成了哪些文件。

如果运行失败：

请停止。

不要修复。

不要修改代码。

只告诉我失败原因。